# 고요한 아쿠아리움 발전 제안서

작성일: 2026-09-05 · 상태: 검토용 제안, 확정 사양 아님

검토 자료: `SPEC.md`, `README.md`, `CLAUDE.md`, `web/src/`의 렌더 루프·물고기·설정·사운드 구현. 사용자가 지칭한 문서는 저장소의 핵심 설계 문서인 `SPEC.md`로 해석했다. 외부 사례는 공식 홈페이지와 개발사 등록 스토어 설명을 확인했다. 실제 게임 플레이, 현재 앱의 브라우저 시각 검증, 성능 측정 및 테스트 실행은 이번 문서 작성 범위에 포함하지 않았다.

## 1. 추천 방향

**‘잠깐 쉬거나 작업할 때 켜두는 나만의 작은 바다’로 발전시키는 것을 추천한다.** 현재 강점인 즉시 감상, low-poly 스타일, 최소 UI, 정적 배포를 유지하면서 분위기 선택과 생물의 존재감을 강화한다.

다음 개발의 우선순위는 **기본 경험 안정화 → 분위기 프리셋과 편안한 감상 → 종별 행동과 관찰 → 개인화·공유**다. 폴리곤 수 증가만으로 만족도가 높아진다고 가정하지 말고, 같은 장면에서 구도·색·움직임이 얼마나 좋아지는지 확인해야 한다.

주요 사용 상황은 두 가지로 잡는다.

- **짧은 휴식:** 페이지를 열고 1~5분 동안 조작 없이 감상한다. 시작 절차와 설명이 짧아야 한다.
- **작업 중 배경:** 옆 화면에 오래 켜둔다. 카메라 고정, 소리 조절, 절전이 중요하다.

어종 수집과 꾸미기를 좋아하는 사용자는 두 번째 확장 대상으로 둔다. 처음부터 경제·육성 시스템을 붙이면 현재의 가벼운 감상 경험과 운영 범위가 크게 달라진다.

## 2. 현재 기반과 먼저 정리할 차이

| 영역 | 자료에서 확인한 상태 | 발전에 주는 의미 |
|---|---|---|
| 그래픽 | 절차적 물고기·산호·해초, 코스틱·포그·광선, 종별 인스턴싱 | 에셋과 렌더러 교체 없이 연출을 개선할 기반이 있다 |
| 설정 | `settings.ts`, `settingsPanel.ts`, `main.ts`에 디테일·개체수·조명·기포·저장 연동 구현이 존재 | 설정 패널을 신규 제안으로 반복하기보다 사용 목적별 프리셋을 얹는다 |
| 생물 행동 | 종별 속도·군집 여부, 응집·분리·배회·경계 회피 코드가 존재 | 어종별 활동 수심과 행동 리듬으로 관찰 재미를 넓힌다 |
| 품질 제어 | 탭 숨김 시 렌더 정지, FPS에 따른 해상도·개체수 하향 | 장시간 사용을 위한 절전 모드와 복구 정책이 다음 과제다 |
| 사운드 | Web Audio 합성 환경음과 켜기/끄기 | 볼륨과 부드러운 종료부터 개선할 수 있다 |
| 문서 | SPEC은 v1.1 설계 확정·수용 기준 미체크, README 로드맵은 v1 중심 | ‘구현 존재’와 ‘검증 완료’를 구분해 상태를 갱신해야 한다 |

이번 확인 당시 `SPEC.md`와 `web/src/config.ts`에는 기존 미커밋 변경이 있었다. 이 제안서는 해당 작업 상태를 기준으로 작성했으며 기존 파일을 수정하지 않았다.

### 기능 추가 전 점검할 구체 항목

다음은 코드 검토로 확인한 점검 대상이다. 사용자 환경에서 발생 빈도까지 확인한 장애 보고는 아니다.

1. **접근성 설정이 3D 장면에도 반영되어야 한다.** 현재 CSS의 `prefers-reduced-motion` 처리는 로딩 표시 속도에 한정되고, `main.ts`의 카메라는 계속 움직인다. 카메라 고정과 효과 감소를 별도로 제공한다.
2. **FPS 측정과 시뮬레이션 시간을 분리한다.** 현재 `dt`를 0.05초로 제한한 뒤 같은 값으로 FPS를 계산한다. 예를 들어 실제 프레임 간격 0.1초도 0.05초로 계산되므로 저성능 정도를 정확히 표현하지 못한다. 실제 경과 시간은 측정에, 제한된 시간은 이동 계산에 사용한다.
3. **페이지 복귀와 종료 흐름을 확인한다.** `pagehide`에서 리소스를 정리하지만 대응하는 `pageshow` 재초기화는 보이지 않는다. 뒤로 가기 캐시 복귀를 재현하고, 복귀 정책을 정한다. 종료 시 예약된 디바운스 리빌드도 취소한다.
4. **음소거 페이드가 실제로 끝나도록 한다.** 현재 `AmbientAudio.stop()`은 볼륨 감소를 예약한 직후 `suspend()`를 호출한다. 오디오 시간 진행이 멈추기 전에 페이드를 완료하는 흐름과 빠른 재토글 처리를 검증한다.
5. **저장 접근 실패를 앱 시작 실패와 분리한다.** 함수 내부 저장 오류는 처리하지만 `window.localStorage`를 읽는 표현식은 함수 밖에 있다. 저장소 획득 자체가 실패하는 환경도 기본 설정으로 시작할 수 있도록 한다.
6. **SPEC 내부의 상충 설명을 정리한다.** §6.5.6·AC-4의 숨긴 메시 드로우콜 설명, §6.5.3의 교체·dispose 순서 설명을 실제 동작과 실측 기준으로 통일한다. AI·기포 갱신까지 모두 GPU에서 처리된다는 §4 설명도 CPU 업데이트 코드에 맞춘다.

## 3. 유사 서비스에서 참고할 점

아래 ‘확인한 특징’은 공식 자료에 근거하며, ‘적용 제안’은 이 프로젝트를 위한 해석이다. 매출·이용자 성과나 힐링 효과를 입증한 비교는 아니다.

| 사례 | 확인한 특징 | 아쿠아리움 적용 제안 |
|---|---|---|
| [어비스리움](https://www.abyssrium.com/Abyssrium_KO/index_ko.html) | 산호석을 키우고 생물을 불러 심해를 가꾸는 경험, 클래식 음악을 활용한 시리즈 소개 | 중심 산호와 주변 생물로 시선이 머무는 구도를 만들고, 테마별 색·빛·소리를 일관되게 설계한다 |
| [Chillquarium](https://store.steampowered.com/app/2276930/Chillquarium/) | 물고기 성장·수집·색상 변형, 수조 꾸미기, 낮밤 주기 | 어종 관찰 기록, 팔레트 변형, 낮밤 분위기를 참고한다. 거래·부스터팩 경제는 이번 방향에서 제외한다 |
| [Viridi](https://store.steampowered.com/app/375950/Viridi/) | 실시간으로 자라는 작은 다육식물 화분과 다시 방문할 조용한 공간 | 산호의 느린 변화나 작은 발견으로 공간에 애착을 만든다. 접속하지 않았다는 이유로 손실이 생기지 않게 설계한다 |
| [A Soft Murmur](https://asoftmurmur.com/about/) | 사용 환경에 맞게 여러 환경음을 혼합하는 서비스 | 바다·잔잔한 물소리 등 소수 레이어를 조절하고 취향을 저장한다. 먼저 단일 환경음의 음량과 전환 품질을 완성한다 |

공통적으로 가져올 원칙은 **내 취향으로 고르는 공간, 천천히 발견하는 변화, 다시 열기 쉬운 경험**이다. 출석 보상·연속 접속·경쟁 순위는 현재 제품 목표에 맞지 않아 우선순위를 두지 않는다.

## 4. 기능 우선순위

규모는 1인 개발을 가정한 상대 추정이다. 소는 기존 모듈의 작은 확장, 중은 여러 모듈과 UI 변경, 대는 새로운 상태·콘텐츠 체계를 의미한다. 확정 일정은 아니다.

| 순위 | 제안 | 기대 가치 | 규모 | 선행 조건 |
|---|---|---|---|---|
| P0 | 복귀·저장·오디오·성능 측정 안정화 | 첫 방문과 장시간 감상의 신뢰도 | 중 | 현재 동작 재현·기준선 기록 |
| P1 | 분위기 프리셋 3개 | 설정 지식 없이 취향을 선택 | 소~중 | 기존 설정 경로 재사용 |
| P1 | 카메라 고정·UI 숨김·절전 모드 | 오래 켜둘 수 있는 감상 경험 | 중 | 품질 정책과 입력 복귀 설계 |
| P1 | 종별 유영 차이와 구도 개선 | 바라볼 이유를 강화 | 중 | 동일 시드 비교·행동 테스트 |
| P2 | 물고기 선택·짧은 도감 | 생물에 대한 관심과 애착 | 중 | 선택 판정·읽기 쉬운 정보 UI |
| P2 | 분위기 저장·이미지 및 설정 공유 | 내 공간을 보관하고 다시 방문 | 중 | 저장 스키마·프리셋 안정화 |
| P2 | 선택형 먹이 반응 | 짧고 부담 없는 상호작용 | 중 | 행동 상태와 개체수 상한 |
| P3 | 낮밤 전환·추가 생태 테마 | 반복 방문 시 변화 | 중~대 | 테마별 연출·성능 검증 |
| P3 | PWA·오프라인 이용 | 자주 쓰는 사용자의 재접근 | 중 | 재방문 수요·캐시 갱신 설계 |

### 4.1 분위기 프리셋: 가장 먼저 체감할 개선

처음부터 여러 슬라이더를 조절하게 하지 않고, 설정 상단에 **맑은 산호초 / 고요한 바다 / 은은한 저녁**을 둔다. 최초 진입은 지금의 기본 장면으로 바로 시작하고, 프리셋 선택은 선택 사항으로 둔다.

- 1차는 현재 가능한 조명 세기·개체수·기포 조합을 활용한다. 기존 스키마에 없는 색온도·포그 색·사운드까지 이미 지원한다고 가정하지 않는다.
- 2차에 테마 팔레트와 카메라 속도를 확장한다. 장면 분위기와 기기 성능 설정을 구분하여 ‘저녁’을 골랐다는 이유로 High 디테일을 강제하지 않는다.
- 수동 조절 시 ‘사용자 설정’으로 표시하고 기본값 복원 동작을 제공한다. 프리셋 변경으로 소리를 자동 재생하지 않는다.

**수용 기준:** 프리셋 3개가 기존 변경 경로를 통해 반영되고 재방문에도 유지된다. 빠른 연속 선택은 최종 상태로 수렴한다. 처음 방문한 사람이 설명 없이 분위기를 바꿀 수 있는지 짧은 사용성 확인을 한다.

### 4.2 감상 모드와 절전

카메라는 ‘천천히 이동 / 고정’ 두 선택부터 시작한다. UI 숨김 상태는 터치·포인터·키보드로 다시 열 수 있게 하고, 설정 조작 중에는 자동으로 숨기지 않는다. 시스템의 움직임 감소 요청은 초기 카메라 설정에 반영하며 직접 바꿀 수도 있게 한다.

절전 모드는 30fps 목표와 낮은 DPR을 함께 적용하는 방식부터 검토한다. 기존 40fps 미만 하향 정책은 절전 모드와 충돌하므로 모드별 임계값을 분리한다. 자동 품질 복구는 안정적인 프레임이 충분히 지속될 때 한 단계씩 실행하여 화질이 반복해서 오르내리지 않게 한다.

**수용 기준:** 고정 모드에서 카메라 위치가 시간에 따라 움직이지 않는다. 키보드만으로 모드 선택과 설정 닫기가 가능하다. 절전 모드가 의도한 30fps를 성능 장애로 오인하지 않는다. 사용자가 정한 개체수와 자동 축소 배율은 계속 분리한다.

### 4.3 자연스러운 물고기: 개체수보다 차이

현재 군집 로직에 다음 항목을 데이터로 확장한다.

- **활동 수심:** 바닥 가까이, 중층, 위쪽을 선호하는 차이를 만든다.
- **회전과 속도 리듬:** 방향을 바꾸는 속도에 상한을 두고 잠깐 느려졌다가 유영하는 리듬을 준다.
- **서식 지점:** 특정 산호 주변에 머무르는 정도를 종별로 둔다. 현재 `activityRadius`는 생성 위치에 쓰이므로 지속적인 활동 반경 제어는 별도로 설계한다.
- **장애물 회피:** 실제 산호의 모든 삼각형 대신 소수의 단순 회피 영역으로 시작한다.

종별 인스턴싱과 시드 기반 재현성은 유지한다. 현재 분리 계산에는 종 내부 이중 루프가 있으므로 대규모 개체수 확장은 CPU 비용을 측정한 뒤 판단한다. 종이 늘어도 모두 같은 몸체 계열에 색만 다르면 차별성이 약하므로, 큰 어종 추가 전에 실루엣과 행동 차이를 먼저 확인한다.

**수용 기준:** 고정 시드에서 60초간 경계 이탈·비정상 좌표·급격한 방향 반전 여부를 확인한다. 선호 수심과 최대 회전량은 순수 로직 테스트로 검증한다. 개선 전후 영상을 보고 사용자가 움직임 차이를 구별하는지 확인한다.

### 4.4 선택과 관찰 기록

물고기를 누르면 화면 가장자리에 이름·특징 한두 문장·‘관찰한 어종’ 기록을 보여준다. 모바일에서 작은 물고기를 누르기 어렵다면 도감 목록에서도 같은 정보를 열 수 있게 한다. 기록은 브라우저에 저장하고, 모든 어종을 직접 고를 수 있는 현재 자유도를 잠금으로 바꾸지 않는다.

실제 생물 정보를 쓰려면 종명과 설명을 별도로 검증한다. 현재의 디자인용 명칭을 검증된 생물 분류처럼 표시하지 않는다. 선택 시 카메라가 갑자기 이동하지 않도록 하고, 추적 카메라는 후속 옵션으로 둔다.

**수용 기준:** 선택한 종과 표시한 카드가 일치한다. 클릭 이외 경로로도 정보를 볼 수 있다. 배경을 누르거나 Esc를 누르면 닫힌다. 숨겨진 어종이나 비활성 인스턴스를 잘못 선택하지 않는다.

### 4.5 나만의 바다 저장·공유

첫 단계는 저장 슬롯 3개와 UI 없는 PNG 내보내기다. 이후 **테마 ID·허용된 설정·시드·버전**을 담은 링크를 제공한다. 공유는 동일한 분위기와 초기 배치를 재현하는 것으로 정의하고, 이동 중인 모든 물고기의 순간 위치까지 복제하는 기능은 별도로 본다.

정적 앱에서도 설정 기반 링크는 가능하다. 링크 길이를 제한하고 값·버전을 검증하며, 알 수 없는 어종·깨진 링크에는 기본값을 사용한다. 링크를 열었다고 기존 개인 설정을 즉시 덮어쓰지 않고 ‘내 설정으로 저장’ 동작을 둔다. PNG는 필요할 때 캡처하는 흐름으로 구현하고 캡처 때문에 상시 렌더링 비용을 올리지 않도록 측정한다.

**수용 기준:** 새 브라우저에서 링크를 열어 같은 프리셋과 설정을 확인한다. 손상된 링크로 앱이 중단되지 않는다. 이미지 저장 성공과 미지원·실패 상태를 사용자에게 알린다.

### 4.6 먹이 주기는 반응만 제공

원하는 사용자만 먹이를 놓으면 근처 일부 물고기가 잠깐 접근하고 다시 유영한다. 굶주림·죽음·성장 손실은 도입하지 않는다. 연속 입력으로 입자와 행동 요청이 쌓이지 않도록 동시에 존재하는 먹이 수를 제한한다.

**수용 기준:** 정해진 시간이 지나면 기본 행동으로 복귀하고, 빠른 반복 클릭에도 메모리와 개체수가 계속 증가하지 않는다. 아무 조작 없이 감상하는 경험은 처음부터 완성되어 있어야 한다.

## 5. 시각·사운드 개선 원칙

High 디테일은 기존 합의된 SPEC 목표를 따라 검증하되, 그 이후에는 삼각형 증가율을 품질 목표로 삼지 않는다. **작은 화면에서도 알아보이는 실루엣, 중앙의 시선 집중 지점, 앞·중간·뒤의 깊이 차이**를 먼저 비교한다.

같은 시드와 카메라로 기본·High·모바일 장면을 나란히 확인한다. 물고기 눈과 지느러미 가독성, 산호와 배경의 색 분리, 광선이 물고기를 가리는 정도를 조정한다. 새로운 후처리 효과는 기존 예산 내에서도 기기별 프레임 비용을 확인한 뒤 채택한다.

사운드는 **음량 슬라이더 → 켜기/끄기 페이드 → 추가 레이어 2~3개** 순서로 확장한다. 숨긴 탭에서 오디오는 계속 들을지 별도 설정으로 결정하고, 기본은 감상 목적에 맞게 설명한다. 녹음 파일을 추가할 때는 출처·이용 조건을 기록한다.

## 6. 실행 계획과 완료 조건

아래 기간은 개발자 1명이 기존 구조를 이해하고 있다는 가정의 계획 예시다. 브라우저 문제와 아트 조정량에 따라 달라질 수 있으며, 각 단계 완료 조건을 일정에 우선한다.

| 단계 | 예상 기간 | 결과물 | 다음 단계로 넘어가는 조건 |
|---|---|---|---|
| A. 기준선 정리 | 2~4일 | SPEC 상태 정리, 기본·최대 설정 측정, 복귀·저장·소리 점검 | 주요 시나리오 재현 및 수정 후 확인 |
| B. 감상 경험 | 약 1주 | 프리셋 3개, 카메라 고정, 음량, UI 숨김 | 모바일·키보드 사용 가능, 기본 장면 회귀 없음 |
| C. 생물 존재감 | 1~2주 | 종별 수심·회전·서식 지점, 구도 조정 | 행동 테스트와 동일 장면 비교 통과 |
| D. 애착과 공유 | 1~2주 | 관찰 카드, 저장 슬롯, PNG·설정 링크 | 저장·공유 왕복 확인, 초기 사용자 평가 |
| E. 수요에 따른 확장 | 별도 산정 | 먹이 반응, 낮밤, 테마 또는 PWA | 사용자가 실제 원하는 확장임을 확인 |

**바로 시작할 다음 작업은 A와 B다.** 후보 기능을 모두 동시에 구현하지 않는다. 약 2주 범위로 기준선과 감상 경험을 먼저 완성하면 이후 투자 판단에 사용할 결과가 생긴다.

실제 구현 시에는 기존 SDD/TDD 원칙에 따라 선택한 기능을 `SPEC.md` 요구사항·결정 로그·수용 기준에 먼저 반영한다. 순수 상태·직렬화·행동 로직은 테스트부터 작성하고, 화면과 소리는 프로덕션 빌드 미리보기에서 확인한다.

## 7. 검증과 성공 판단

현재 수치는 아직 측정하지 않았다. 다음은 도입할 검증 기준과 실험 방법이다.

| 질문 | 검증 방법 | 판단 기준 |
|---|---|---|
| 첫 방문이 쉬운가? | 초기 사용자 5~8명에게 사전 설명 없이 열어보게 함 | 대부분이 도움 없이 감상·소리·분위기 변경 가능 |
| 오래 켜두기 편한가? | 휴대폰과 노트북에서 15분 감상 | 발열·팬 소음·움직임 불편의 반복 보고가 없는지 확인 |
| 성능 예산을 지키는가? | 기본·최대 설정의 렌더 통계 및 프레임 시간 기록 | SPEC의 드로우콜·삼각형 예산 준수, 지원 기기에서 목표 프레임 유지 |
| 기본 경험이 안정적인가? | 저장 불가·손상 데이터·탭 전환·뒤로 가기·빠른 설정 변경 | 시작 실패·검은 화면·반복 리소스 증가 없음 |
| 추가 기능이 가치가 있는가? | 기존/개선 장면 비교와 재사용 의향 질문 | 반복적으로 선호되는 변화부터 확장 |

성능 기록에는 기기·OS·브라우저·해상도·DPR·설정·실제 표시 개체수와 프레임 시간 중앙값/p95를 남긴다. 테스트 대상은 데스크톱 주요 브라우저, iOS Safari, Android Chrome의 실제 기기 조합으로 먼저 정한다. 30fps 절전 모드와 기본 모드의 목표는 구분한다.

체류 시간만 늘리는 것을 목표로 두지 않는다. 짧게 쉬고 닫는 것도 성공적인 사용이다. 초기에는 관찰과 인터뷰로 충분하며, 이후 분석이 필요하면 보이는 탭의 감상 시간·프리셋 선택·다시 열기 같은 최소 지표부터 설계한다. 브라우저 저장만으로 측정한 재방문은 다른 기기나 저장 삭제를 식별하지 못한다. 의료적 효능을 측정하거나 보장하는 지표로 해석하지 않는다.

## 8. 뒤로 미룰 것과 도입 조건

- **WebGPU 전환:** 현재 목표 기기에서 병목을 측정하고 기존 렌더러 개선으로 해결하기 어려울 때 비교 실험한다. 전환 자체를 사용자 가치로 보지 않는다.
- **Hono API·계정·클라우드 저장:** 기기 간 동기화 또는 서버에 남아야 하는 공유 상태의 수요가 확인될 때 도입한다. 로컬 설정과 프리셋에는 현재 정적 구조를 사용한다.
- **유리병 메시지·방문자 커뮤니티:** 읽을 콘텐츠의 가치, 신고·삭제·스팸 처리와 운영 주체가 정해졌을 때 독립 기능으로 검토한다.
- **경제·출석·희귀도 중심 성장:** 사용자가 수집 게임을 원하는지 별도로 확인하기 전에는 도입하지 않는다.
- **대규모 생태 시뮬레이션·자유 배치 편집기:** 단순 서식 지점·선택형 테마의 반응을 먼저 확인한다. 편집기에는 저장·되돌리기·모바일 배치 UI까지 필요하다.
- **유료화:** 재사용 가치가 확인된 후 추가 테마·꾸미기 같은 선택형 콘텐츠를 검토한다. 지금은 결제 시스템보다 감상 경험의 완성도를 우선한다.

## 9. 구현 착수용 체크리스트

- [ ] 현재 v1.1 구현과 SPEC 수용 기준을 대조해 검증 완료 여부를 기록한다.
- [ ] 기본·최대 설정의 동일 시드 화면과 기기별 성능 기준선을 남긴다.
- [ ] 페이지 복귀·저장 접근·음소거 페이드·FPS 측정 점검을 완료한다.
- [ ] 프리셋 3개와 카메라 고정의 사양·저장 정책을 SPEC에 추가한다.
- [ ] 프리셋 전환·저장·설정 복원에 필요한 테스트를 먼저 작성한다.
- [ ] 프로덕션 미리보기에서 모바일·키보드·오디오·장시간 감상을 확인한다.
- [ ] 초기 사용자 관찰 결과를 기록하고 다음 단계 범위를 선택한다.

참고 링크는 2026-09-05 확인했다. 외부 사례의 설명은 해당 공식 자료에 근거하며, 우선순위·일정·기대 가치는 이 프로젝트에 대한 제안이다.
